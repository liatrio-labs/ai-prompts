# ci: autoupdate: schedule: quarterly
fail_fast: false
default_install_hook_types: [pre-commit, pre-push, commit-msg]
default_stages: [pre-commit]

# Optional: Enable branch protection by creating .pre-commit-config.local.yaml with:
#   repos:
#     - repo: https://github.com/pre-commit/pre-commit-hooks
#       rev: v6.0.0
#       hooks:
#         - id: no-commit-to-branch
#           args: ['--branch=main', '--branch=master']
# Then run: pre-commit install with SKIP=no-commit-to-branch if needed

repos:
  - repo: local
    hooks:
      - id: check-docs-drift
        name: check documentation drift for skills and script references
        entry: python scripts/check_docs_drift.py
        language: system
        pass_filenames: false
        files: "^(README\\.md|CONTRIBUTING\\.md|docs/development\\.md|AGENTS\\.md|skills/.*|scripts/check_docs_drift\\.py)$"

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      -   id: check-yaml
      -   id: end-of-file-fixer
      -   id: trailing-whitespace
      -   id: check-toml
      -   id: check-added-large-files

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.45.0
    hooks:
      - id: markdownlint-fix
        exclude: "CHANGELOG.md|LICENSE"

  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.23.0
    hooks:
      - id: commitlint
        name: lint commit message for Conventional Commits compliance
        additional_dependencies:
          - '@commitlint/config-conventional'
        stages: [commit-msg]
        args:
          - "--extends=@commitlint/config-conventional"

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.29.0
    hooks:
      - id: gitleaks
        name: scan for committed secrets with gitleaks
        args:
          - "--redact"
